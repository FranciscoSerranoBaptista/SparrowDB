{
  "name": "sparrowdb",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "docs:dev": "pnpm --filter @sparrowdb/docs dev",
    "docs:build": "pnpm --filter @sparrowdb/docs build"
  }
}
